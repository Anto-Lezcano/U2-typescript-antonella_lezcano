enum Color {
  Red = "Red",
  Green = "Green",
  Blue = "Blue",
}

function mostrarColor(color: Color) {
  console.log(`Color ${color}`);
}

mostrarColor(Color.Red);
mostrarColor(Color.Green);
mostrarColor(Color.Blue);
